name: Code Review

on:
  pull_request:

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run AI Bot
        uses: actions/github-script@v7
        with:
          script: |
            console.log("AI Code Review Bot running...");
